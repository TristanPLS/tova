# --- Rust / Cargo ---
/target
**/target
**/*.rs.bk
# Cargo.lock EST versionne : le workspace produit des binaires (tova-node, tova-cli, tova-verify).

# --- WASM (jalon J4) ---
/pkg
**/pkg
*.wasm

# --- Secrets / environnement (JAMAIS committer) ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
# Parts de cle des garants (DKG a seuil) — hors depot
*.share
secrets/

# --- Donnees runtime ---
*.sqlite
*.sqlite-*
/data
*.log

# --- Divers OS / editeurs ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Cache d'outillage local (jamais versionne) ---
.impeccable/
