[package]
name = "tova-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Coeur cryptographique de TOVA : signature de cercle linkable + key image. Squelette J0, implementation en J1."
publish = false

[dependencies]
# Ajoutees au jalon J1 (voir docs/spec-crypto.md) :
#   curve25519-dalek (Ristretto255, >=4.1.3), merlin (Fiat-Shamir),
#   subtle (constant-time), zeroize, rand_core, getrandom.
